//literal objects 
let employee = {
    id: 1,
    name: 'Subramanian',
    address: {
        city: 'Coimbatore'
    }
}
console.log(`Id ${employee.id} name ${employee.name} city ${employee.address.city}`)